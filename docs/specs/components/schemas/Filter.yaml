type: object
description: |
  フィルタ定義

  型安全なフィルタ条件を定義します。
  各フィールドに対して直接値を指定するか、MongoDBOperatorsを使用できます。
properties:
  $and:
    type: array
    description: AND条件
    items:
      $ref: './Filter.yaml'
  $or:
    type: array
    description: OR条件
    items:
      $ref: './Filter.yaml'
additionalProperties: true
