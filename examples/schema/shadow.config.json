{
  "$schemaVersion": "2.0",
  "$generatedFrom": "schema.ts (MySchemaRegistry)",
  "database": {
    "name": "myapp",
    "timestamps": {
      "createdAt": "createdAt",
      "updatedAt": "updatedAt"
    }
  },
  "resources": {
    "articles": {
      "shadows": {
        "title": { "type": "string" },
        "status": { "type": "string" },
        "author": { "type": "string" },
        "createdAt": { "type": "datetime" },
        "updatedAt": { "type": "datetime" }
      },
      "sortDefaults": {
        "field": "updatedAt",
        "order": "DESC"
      }
    }
  }
}
